# Pinned for reproducibility (environment used to produce the paper's results).
numpy==1.26.4
scipy==1.13.1
matplotlib==3.9.2
pandas==3.0.1
transformers==5.5.4
datasets==5.0.0
# torch CPU build; on CI install from the CPU index (see workflows) to avoid CUDA wheels.
torch==2.12.0
pytest==8.3.2
